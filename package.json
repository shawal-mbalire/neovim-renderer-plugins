{
  "name": "neovim-renderer-plugins",
  "version": "0.1.0",
  "description": "Serverless Markdown, ipynb, and Image renderer plugins for Neovim",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:unit": "bun test tests/unit",
    "test:watch": "bun test --watch",
    "lint": "bun run --bun tsc --noEmit"
  },
  "keywords": ["neovim", "markdown", "ipynb", "image", "renderer"],
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
